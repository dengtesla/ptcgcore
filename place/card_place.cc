#include "card_place.h"
#include "place/stage_watcher.h"

namespace ptcgcore {

// int MonsterPile::GetUsableFunc(std::vector<void(*)()> functions) {
//   // 在 monster 里搜索可用卡片
//   for (const auto& cardptr: monsters) {
//     cardptr->GetUsableFunc(functions);
//     auto stage_watcher = StageWatcher::getInstance();
//   }
//   return true;
// }

}